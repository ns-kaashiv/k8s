apiVersion: v1
kind: LimitRange
metadata:
  name: container-limit-range
  namespace: prod
spec:
  limits:
  - type: Container
    default:
      cpu: "250m"
      memory: "512Mi"
    defaultRequest:
      cpu: "125m"
      memory: "256Mi"
